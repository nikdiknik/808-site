[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "./node_modules/.bin/next start -H 0.0.0.0 -p ${PORT:-3000}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
